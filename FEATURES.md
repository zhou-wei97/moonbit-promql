# 功能与兼容性边界

0.3.0 新增向量匹配 bool/on/ignoring/group_left/group_right，offset/@，复合时长、子查询，
前后置聚合分组、参数聚合、字符串节点和更多常用函数类型签名。
语法树保留新字段；类型检查拒绝标量匹配、缺 bool 的标量比较、集合分组、重复时间修饰符等非法组合。
网页与 CLI 同时解析和检查类型。公共 API 清单和具体范围见 README。

本轮 12 项项目 JS 测试通过；修补括号内重复修饰符和保留关键字后，4 组相关测试再次通过。
官方 promtool 对照脚本已准备，二进制下载过慢后停止，尚未执行官方接受/拒绝对照。

剩余差距：完整数字/字符串词法、全部函数、RE2 语义与空标签约束、UTF-8 引用标识符、
源码位置诊断、新版/实验性运算符和时长表达式。不能判定全面追平。
这是查询解析库，不含指标查询执行器；不以是否具备数据库运行时衡量解析库完成度。

独立仓库与构建目录、已有浏览器/CLI/文档/本地测试/CI 配置保留。
CI 配置不是远端运行证明，旧 evidence 和 ZIP/bundle 为历史快照，本轮未重打包。
