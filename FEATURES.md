# 功能与兼容性边界

0.4.0 对照 Prometheus 3.14.0，补全 90 个函数签名、数字和字符串词法、引用标识符、原始字节保留、RE2 静态语法/空匹配、选择器约束与 info 例外、定位错误和四个独立实验开关。支持时长表达式、扩展范围、二元填充值。parse 现在同时执行静态类型检查。

验证：24 组 MoonBit 测试在 JS/Wasm-GC 分别通过，13 组结构化入口/CLI 检查，3,213 个真实独立官方解析器用例比较接受/拒绝、类型、规范化 AST 和正则空匹配。参考库固定到 v0.314.0，源码依赖校验和、保存向量和运行报告在 evidence/ 与 tools/prometheus-reference/。

正则模块只验证语法与空串匹配，不执行任意字符串匹配。函数只检查静态参数类型，不评估指标。资源限制、时间整数转换边界和版本范围见 README。错误定位中的类型错误仍为整个查询区间；尚缺 AST 每个节点的源码区间、完整格式化/遍历/重写 API、多版本和生产性能证明，因此仍不能判定全面追平。

无上传，远端 CI 未执行，旧 ZIP/bundle 未重打包。
