export default {"slug": "promql", "title": "PromQL 查询解析器", "scope": "向量选择、标签匹配、范围、函数和二元表达式 AST", "source": "https://prometheus.io/docs/prometheus/latest/querying/basics/", "limitations": "仅语法 AST：不执行指标查询，不做函数签名/向量类型/正则语义校验；不含 offset、@、子查询、bool/on/ignoring/group_left 等修饰符；范围时长限单一单位。", "recommendation": "优先：有明确查询工具价值，可补完整语法与标准错误位置。", "version": "0.1.0", "example": "sum by (job) (rate(http_requests_total{method=\"GET\",status!=\"500\"}[5m])) / 2"};
