export default {"slug": "promql", "title": "PromQL 查询解析器", "scope": "向量选择、标签匹配、范围、函数和二元表达式 AST；增加标量/瞬时向量/区间向量类型检查和常见函数签名校验，修复非法 UTF-16 崩溃", "source": "https://prometheus.io/docs/prometheus/latest/querying/basics/", "limitations": "仅文档列出的函数与语法子集；不执行查询，不支持完整匹配修饰符", "recommendation": "优先：有明确查询工具价值，可补完整语法与标准错误位置。", "version": "0.2.0", "example": "sum by (job) (rate(http_requests_total{method=\"GET\",status!=\"500\"}[5m])) / 2"};
